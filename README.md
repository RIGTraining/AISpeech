# AISpeech

pip install speechrecognition pydub


pip install openai-whisper



pip install moviepy==1.0.3 // left to install


pip install django django-cors-headers pillow torch torchvision torchaudio transformers accelerate librosa soundfile
# For async (recommended): celery redis